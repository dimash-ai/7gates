# Findings: <topic>

> Output of the **explore gate** (`/gate-explore <topic>`). Two independent takes — Opus and GPT —
> synthesized. Small and findings-first; raw per-model answers live in `.ai/scratch/` (gitignored).
> Not scored. Date: <YYYY-MM-DD>.

## User story

> One line: who wants this and why. For a pure research topic with no user-facing change, write N/A.

As a <user/role>, I want <capability>, so that <outcome>.

## Current behaviour

> What happens today, grounded — `path/file.ts:42` for code paths, observed behaviour otherwise. No guesses.

- 

## Expected behaviour

> What should happen instead. If the models diverged on it, mark **pending decision** and point at Divergence.

- 

## Acceptance criteria

> Verifiable checks that make the expected behaviour testable — each traces to an answered question.
> These seed `/gate1-think` success criteria or `/gate-design`.

- [ ] 

## Questions

1. <the question this note set out to answer>
2. …

## Consensus

> Both models agree. State once, mark confidence, cite evidence.

- **<finding>** — confidence: High/Medium/Low. Evidence: `path/file.ts:42` or <doc URL>.

## Divergence

> They disagreed or emphasized different things. Both views, then a decision flag for the user.

- **<question>**
  - Opus: <view, briefly>
  - GPT: <view, briefly>
  - **Decision needed:** <what the user must choose, and why it matters>

## Open

> Neither resolved it — name exactly what's missing to close it.

- <open point> — to close: <the specific fact / file / test / person needed>

## Next

> Optional. Where this feeds — e.g. seeds `/gate1-think <feature>`, or a decision now settled.
