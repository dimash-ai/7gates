# Review Verdict

Reviewer: Opus
Step: build
Score: 9.4 / 10
Status: APPROVED

## Slice
4b — backend recurrence + multi-participant (model/schema/service + additive Alembic migration + gen:api) and the client dialog wiring (recurrence select, participants CRM/Other toggle, TaskInfoDialog recurrence display).

## Reason
Slice 4b delivers exactly its scope — `recurrence`/`contact_ids`/`other_participants` on model, schema, service, an additive+reversible autogenerated migration, regenerated `openapi.d.ts`, and the wired dialog/info-dialog — with no scope creep and no unrelated edits. The contract semantics (create-omit→default, update-omit→preserve, `"none"`/`[]` clear, explicit `null`→422, legacy `contact_id`→`[contact_id]` read fallback) are correct, and I independently verified the two highest-risk paths: Pydantic rejects non-string `contactIds` (422, not silent coercion) and the read-normalizer flows through both `TaskRead` and the `EnrichedTaskRead` enrich path. The intentional UI-only CRM `ContactPicker` is implemented consistently with the events feature (CalendarPage.tsx:193-195 mirrors TaskDialog.tsx exactly): the picker holds local state, the save payload never sends `contactIds`, and editing therefore can't clobber a stored value — no half-wired state, no silent data loss.

## Must Fix
None

## Should Consider
- Tests are well-scoped but the green claim for the DB-backed suite (`test_tasks_db.py`/`test_contracts.py`, reported 74 passed) and `alembic check` could only be re-confirmed via the build log here — they skip/error in this review worktree because the local Docker PG isn't running and no `DATABASE_URL`/alembic config is wired (the sanctioned focal docker-compose test loop). The static evidence is strong (migration's 3 columns map 1:1 to the model additions, additive+reversible; live-verified Pydantic rejection and defaults), so this is a re-run note for the verify gate, not a defect.
- The `contact_ids` JSONB element-type guarantee rests on Pydantic `list[str]` strict coercion. It holds today (verified), but a future loosening of model config would silently weaken it; the existing `test_contact_ids_must_be_string_array` test pins it, which is the right safeguard.

## Tests Reviewed
- Read `tests/test_tasks_db.py` (recurrence default/round-trip/validation, omit-preserves/`"none"`-clears/`null`-422; contactIds default/round-trip, omit-preserves/`[]`-clears/`null`-422, non-string→422, legacy `contact_id` surfaces as one-element list + explicit list wins; otherParticipants create/patch/clear-with-null) and the `test_contracts.py` response-shape pin — all targeted at the risky paths.
- Read client `TaskDialog.test.tsx` (sends recurrence on create, persists otherParticipants and never sends a contact reference, prefills recurrence+otherParticipants on edit, asserts no `contactId`/`contactIds`), `TaskInfoDialog.test.tsx` (shows recurrence row when repeating, omits for `none`), `TasksPage.test.tsx` payload updates.
- Ran live: Pydantic `TaskCreate(contactIds=[1,2])` → ValidationError (REJECTED, good); defaults `recurrence=None`, `contact_ids=[]` confirmed. `pytest tests/test_tasks_db.py tests/test_contracts.py` = 74 skipped here (no local PG); relied on build log for the 74-pass + clean-drift result.

## Release Risk
Low
